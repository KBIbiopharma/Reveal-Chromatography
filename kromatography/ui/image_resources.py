""" Pre-built image resources so the time to search for them is spent only
once.
"""
from pyface.image_resource import ImageResource

kbi_logo_img = ImageResource("KBILogo.png")

reveal_chrom_logo = ImageResource("RevealChromLogo.png")

reveal_chrom_icon = ImageResource("RevealChromIcon.png")

splash_screen = ImageResource("Splash.png")
